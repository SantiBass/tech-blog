
{{#if posts.length}}
<section>
  <h2>Your Posts</h2>
  <ol>
    {{#each posts as |post|}}
        {{>dashboard-postinfo post}}
    {{/each}}
  </ol>
</section>
{{/if}}
<button type="submit" class="btn"><a class="link" href="/views/new-post.handlebars">+ New Post</a></button>
<script src="/javascript/adding-post.js"></script> 